# 백엔드를 다른 호스트에 띄웠을 때만 설정합니다.
# 로컬 개발(vite dev + uvicorn)에서는 비워두세요 — Vite 프록시가 처리합니다.
#
# Vercel에 프론트만 올리는 경우 프로젝트 환경변수에 이 값을 넣으세요.
# 끝에 /api 까지 포함해야 합니다.
VITE_API_BASE=https://your-backend-host.example.com/api
