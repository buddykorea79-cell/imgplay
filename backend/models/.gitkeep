얼굴 검출 모델(OpenCV DNN SSD)이 들어갈 자리입니다.

  ./scripts/fetch_face_model.sh

받지 않아도 앱은 정상 동작합니다. 있으면 두 가지가 켜집니다.
  - 1단계: 얼굴이 잡힌 사진에 채도 상한(skin_max) 적용
  - 2단계: 줌·팬의 목표점(POI)을 엣지 무게중심 대신 얼굴로

docker compose는 이 디렉터리를 컨테이너에 바인드하므로, 호스트에서 받아두면
컨테이너를 다시 만들어도 유지됩니다.
